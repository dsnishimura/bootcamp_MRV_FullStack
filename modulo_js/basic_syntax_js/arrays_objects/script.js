let array =  ['string',1,true, ['array1', 'lol'], ['array2']];
console.log(array)


//Função forEach() intera um array

// array;array.forEach(function(item, index){console.log(item, item)
// });

// array.splice(3,4)
// console.log(array)

//objects

var mug = {
    name : 'mug_StarWars',
    color : 'white',
    size : 'M',
    full : true
}

console.log(mug.name)